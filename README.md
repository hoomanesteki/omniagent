# omniagent
OmniAgent is a multi-agent AI data analyst platform that lets users upload any dataset, explore it conversationally, run EDA, SQL, regression, and visualizations — all orchestrated through an intelligent master agent using MCP.
